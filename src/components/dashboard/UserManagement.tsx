// UserManagement has been retired — the dashboard no longer surfaces admin-only
// user management. This empty shim exists only because the file cannot be deleted
// from this sandbox; nothing imports it.
export function UserManagement() {
  return null
}
