# Legacy `/admin` routes

These pages are **deprecated**. `next.config.ts` permanently redirects `/admin/*` to `/dashboard/*`.

Do not add new features here. Remove this folder once external links to `/admin` are no longer needed.
